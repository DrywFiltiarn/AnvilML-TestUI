[
  {
    "id": "P4-A1",
    "description": "anvilml-testui: Artifacts panel HTML form elements",
    "phase": "4",
    "project": "anvilml-testui",
    "prereqs": ["P3-D1"],
    "context": "Replace Artifacts stub in index.html: #artifacts-job-id (text placeholder 'job UUID - blank for all'), #artifacts-list-btn ('List Artifacts'), #artifacts-hash (text placeholder 'artifact hash SHA256 hex'), #artifacts-fetch-btn ('Fetch Image'), #artifacts-response (DIV not pre, class 'response'). Ensure styles.css .response selector covers both pre.response and div.response. python3 check: all five IDs present AND artifacts-response is on a div element. node --check app.js exits 0.",
    "tags": []
  },
  {
    "id": "P4-B1",
    "description": "anvilml-testui: apiFetchBlob, artifact list and image fetch handlers",
    "phase": "4",
    "project": "anvilml-testui",
    "prereqs": ["P4-A1"],
    "context": "In app.js API CLIENT add apiFetchBlob(path): fetch baseUrl+path; non-2xx return {ok:false,status,blob:null}; 2xx return {ok:true,status,blob:await resp.blob()}; network error {ok:false,status:0,blob:null,error:e.message}. PANEL:ARTIFACTS: handleArtifactsList() GET /v1/artifacts with optional ?job_id=, set #artifacts-response textContent=JSON. handleArtifactsFetch() validates #artifacts-hash; apiFetchBlob; create img with createObjectURL(blob), append to cleared div; revoke previous via lastArtifactUrl variable. Wire both in INIT. node --check app.js exits 0.",
    "tags": []
  }
]
